package london.aipartner.echo.core.ui.theme

import androidx.compose.material3.Typography

// Baseline type scale. Phase 6 swaps in the editorial display face; the scale
// here is intentionally a starting point that already reads as considered.
val EchoTypography = Typography()
